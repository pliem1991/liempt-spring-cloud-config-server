spring:
  application:
    name: liempt-spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pliem1991/liempt-environment-config.git
    
server:
  port: ${port:8888}
